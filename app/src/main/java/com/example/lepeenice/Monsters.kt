package com.example.lepeenice

class Monsters constructor(var name : String,var maxLife : Int,var sprite : Int){

    // verifier si le mob est mort

    // verifier à combien de % est la vie de mob pour changer son sprite

        /*fun updateSprite(newSprite: Int)
        {
            if ( currentLife <  currentLife/2)
            {
                sprite = newSprite
            }
        }*/

    // faire spawn un nouveau mob

    // initialiser le du mob

    val monster1 = Monsters("Goblin", 100, R.drawable.mob1)

    //monster1.updateSprite(R.Drawable.mob1_evolved)

}